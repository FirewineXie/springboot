server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #eureka 实例主机名
  client:
    register-with-eureka: false #不把自己注册进入
    fetch-registry: false #不从eureka上获取服务注册信息
    service-url:
      defaultZone: http://localhost:8761/eureka/